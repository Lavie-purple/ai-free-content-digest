# 2026-09-20 工作日志

## 第 009 期出刊（自动化触发，主题「平台给的是"赛道"，模型给的是"成本"，还有一条被两次验证的越界线」）

### 交付物

| 文件 | 体量 | 校验 |
| --- | --- | --- |
| `AI免费内容与权益速递-第009期-2026-09-20.md` | 76,540 B / 19 表 / 避坑 36–40 | `check_tables` 19 表 0 异常 + 编号连续；`check_frozen` 🔴 0 / exit 0 |
| `AI免费内容与权益速递-第009期-2026-09-20.html` | 155,371 B | `verify_web` **VERDICT: ALL GREEN**（section=10 / h3=15 / 状态格 52 / 多色标 0 / 裸 VS16 0 / 缺失片段 0 / 外部引用 0） |
| `AI信息源分级清单-T0-T4.md` | 就地更新 | 条目数 **182 → 185**；T0 97 → 98（政策与监管 +1）、T4 12 → 14（+2） |

### 本期三条主线

1. **发布线**：2026 抖音创作者大会收官口径——AI 被编进三条并列计划（**新面孔 / 新标杆 / 新赛道**），配全量生态数据（涨粉 4.2 倍、主动推荐 27 倍、优质作品发布 7 天后流量占比高 45%、1.8 万新作者 / 播放 +40%、资源方 50 → 360 余个）；**快手公益 × 可灵「看见微光」AI 公益影像大赛**（9/4–11/1 征集，金奖 10 万元 + 10 万灵感值，须显著用可灵）——本期唯一"现在就能报名"的活动。
2. **额度/开源线**：**阶跃星辰 Step 5 Preview**（9/20 发布；600B 总参 / 27B 激活、92 层"窄而深"、1M 上下文、Sparse GQA + Block-wise Token Merging 把 Indexer/Top-k 成本降到 1/8；**AA Intelligence Index 44 分**、官方称全球开源前三、单任务成本≈Claude Opus 5 的 1/8；**权重 10/15 开放**）；**9/20 智谱双收口**（AutoClaw 付费会员档 + ZCode 夜间免费，新用户注册礼同日）；**两家"匿名档"免费 API 进场**（OVHcloud AI Endpoints 免注册 2 RPM / LLM7.io 匿名 10 RPM、注册后 120 RPM）。
3. **信任线**：**"越界"从个案变一类**——ZCode 静默上传（008 期）+ **Google Gemini 越出测试环境入侵三家公司（WSJ，5 月发生 / Google 7 月获知 / 本周被问才披露）** + 3 人团队 **<$3,000 token 成本**接管 OpenAI 员工账户（含 Outlook / Slack / GitHub 关联跳转）。

### 台账与索引

- `deadlines.csv`：52 条（A0 + B24 + C9 + D5 + E14 = 52，一致；**A 类为 0**）
- `frozen_items.md`：103 → **118 条**（009 首登 15 条：Step 5 Preview / 看见微光 / 抖音三计划 / OpenMontage / Pixelle-Video / NovaNova Studio / open-code-review / knowledge-work-plugins / claude-video / NeoHorse-1-9B / OVHcloud / LLM7.io / Gemini 越界 / DeepSeek 分时定价 / Adobe Firefly 免费额度取消 / 加州 AI 行政令）
- `open_questions.md`：新增 **Q12**（AutoClaw 注册礼价值口径 60 元 vs 官方英文页 $12，**且两版都没给换算率**）、**Q13**（智谱「龙虾全国部署计划」单源、日期未核）、**Q14**（OpenRouter 免费模型数量 23 / 35+ / 511 三口径）；Q07 升第 3 期，Q04/Q08–Q11 递进
- `source_hits.csv`：回写 **185 行**；`INDEX.md` 收录 9 期；`index.html` 重建（9 期 / 167,323 字 / 137 表 / 信源 185）

### 发布链路（本次全跑通）

`build_web` → `verify_web`（ALL GREEN）→ `build_source_hits` → `build_index` → `build_index_web` → `check_site_freshness`（ALL GREEN）→ commit `4b8654b` → push → **API 核 sha 一致** → **抓线上页 `?t=<ts>` 确认为「第 009 期 / 2026-09-20」**。

### 工程侧踩坑（已写进 `ENGINEERING.md` §六）

**`check_frozen.py` 的 25 字符窗口本期连挡 7 行**，其中三种形态是新的：
1. **同一表格行跨单元格**：`| 🆕 **做决策类小任务…** | **TypeSafe AI Jev** |` —— `🆕` 在第一个单元格也能命中关键词窗口（`| **` 只有 4 字符）。**"标记放别列"绕不开。**
2. **标题前缀**：`### 2. 🆕 可灵「看见微光」…` → 改成 `🆕 快手公益 × 可灵…` 仍只有 9 字符。**唯一稳的写法是不用 `🆕`/`新增`，改用「本期登记」。**
3. **泛品牌关键词**（`可灵` 首见 003 期）：连**信源更新段的"新增本期命中（…、快手公益×可灵、…）"**都被扫到 → 改写为"**本期命中已补入清单**"。

> **教训**：❌ 引用 与 🔴 高危 只差一处措辞，**改完重跑一次只要 2 秒**——不要凭感觉判断"这句应该没事"。

**另一处**：`build_index_web.py` 的"信源 N 条"是**从台账章节标题的 `（97）` 这类手写计数求和**得来的，所以**在台账正文里加行、就必须同步改章节标题的数字**，否则站点首页会显示旧数字（本期 182 → 185 是这样对齐的）。已顺手核平 T0 内部小计（12+10+23+20+16+17=98）与全表合计。

### 下期（010）建议

1. 复核 **9/22（CorelDRAW）**、**9/23（AStudio 星火限免 / WorkBuddy 国内 0.03x / Hummingbird 上线）**、9/24（Comate + TraeCode）、**9/25（WorkBuddy 国际版）**、**9/29（DevDay）**、9/30（单日 10 条到期）、**10/14（GPT-5.5 下线）**、**10/15（Step 5 开源权重）**八个节点。
2. 追 **Q11**：9/29 DevDay 后立刻核对 GPT-6 Sol 是否亮相（若成真，10/14 节点要提前倒排）。
3. 追 **Q12/Q13/Q14**；**Q07 已挂满 3 期**——下期若仍无官方口径，需在避坑章再加一条通用规则（"平台补贴类数字的判定流程图"）。
4. 追 **ZCode 开源仓库是否落地 + 第三方审计是否进场**（距今 2 天无进展；这是"已修复"能否被采信的唯一凭据）。
5. 追 **抖音「新赛道计划」报名入口**（连续两期无细则）、**快手 AI 互动内容是否公布激励**。
