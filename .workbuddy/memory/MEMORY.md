# 项目长期记忆 · AI 免费内容与权益速递

## 展示规则（用户明确定下的约定，2026-09-16）

- **`AI信息源分级清单-T0-T4.md` 不对外展示**。它是**内部信源管理台账**，只用于我自己扫描时的路由与去重。
- 每次出刊**只 `present_files` 那一份日报**（`AI免费内容与权益速递-第NNN期-YYYY-MM-DD.md`）。
- 台账**照旧维护**（新增信源、缺口、能力说明都要就地更新），只是**不进展示列表**。
- **只有用户明确要求看台账时才展示它**——用户说「给我看信源清单」之类时才 present。

## 出刊约定

- 每期仍只做**增量 + 纠错**，不重复往期已固化内容。
- 报告结构固定：摘要（3 秒版）→ AI 免费内容发布 → 免费内容生成工具 → 常驻免费额度与 API → 限时活动与截止时间表 → 本期纠错清单 → 避坑提示（编号跨期连续）→ 按任务选型速查 → 信源体系更新 → 来源与核查说明。
- 发布线与「免费内容」线是必备两章，不能省。
- 交付前用脚本核对所有表格列数（逐行数 `|` 与分隔行比对），并检查避坑编号与前一期连续。
- 截止时间表要标 **生效中 / 换挡日 / 已结束** 三态，并优先复核「最近 3 天内的节点」。
- 涉及多站点口径的（如 WorkBuddy 国内站 vs 国际站、混元新老平台），必须**显式拆开写**，不能合并成一句。

## 网页版编译器与校验器（2026-09-17 建立）

- 编译器：`.workbuddy/build_web.py`。**常驻保留，不要删**。
- 用法（006 期起**命令行优先**；2026-09-17 起**不传参自动取日期最新一期**，彻底不用手改路径）：
  ```bash
  python .workbuddy/build_web.py                                    # 自动取最新
  python .workbuddy/build_web.py "AI免费内容与权益速递-第006期-2026-09-17.md"   # 指定期
  ```
  issue 号、日期、主题行从 md 的 `#`/`>` 自动提取。
- 校验器：`.workbuddy/verify_web.py`。用法同上（不传参自动取工作区里日期最新的一期）。**每次编译后必须跑一遍，判定标准是 `VERDICT: ALL GREEN`。**
  四项：① 表格列数/行数逐表比对；② 全文片段保真（去掉标点后 ≥12 字符的片段必须能在 html 里找到）；③ 标签闭合/占位符/外部引用应为 0；④ 第四章状态格每格只应有一个色标且不留裸 U+FE0F。
  **注意**：保真比对必须**排除标题行**——html 里的章节号渲染成 `01/02…`，md 里是 `一/二…`，直接比会假报缺失。比对前统一"只保留中英文数字"再比，能绕开所有 markdown 标点/emoji 的对称性问题。
- 产物特征：单文件自包含（**0 外部引用**，约 98 KB）、离线可开、可直接打印 A4。
- 已实现能力（改这个脚本时别弄丢）：顶部导航 + 侧栏目录双高亮、全文检索（带命中计数与上下条跳转）、第四章按状态筛选（必办/今日换挡/生效中/口径存疑/已结束）、明暗主题（localStorage 记忆 + prefers-color-scheme 兜底）、阅读进度条、移动端目录抽屉、打印样式。
- **状态判定规则（006 期修订，别再改回去）**：**以状态格里最先出现的色标为准**。旧实现是 `⚠️` 无条件优先，会把"🟢 生效中……⚠️ 另有口径"这类行错判成"口径存疑"并从"生效中"筛选里漏掉。现在只有 `⚠️` 出现在任何颜色标之前才判 warn；`已结束` 文字仍强制 over。
  **同时必须摘干净色标**：`⚠️` 是 `U+26A0 + U+FE0F`，只 replace 基字符会在正文里留一个裸变体选择符（肉眼看不见、但确实渲染出来）。统一用 `ST_ANY_RE` 摘除。
- **设计规范**（详见 `~/.workbuddy/skills/frontend-design`）：暖纸质底 + 墨黑正文 + 单一信号色（赭红 #B23A1E）；标题字号用 `em` 相对单位，**不要用 `clamp(vw)`**；装饰性文字必须显式给 `opacity`/背景色，不能依赖 `z-index:-1`（会被 body 背景吞掉）。

## 工程基建（2026-09-17 全量建立）

### 版本控制

- 本工作区已是 git 仓库，远端私仓 **`Lavie-purple/ai-free-content-digest`（private）**。每期出刊 + 编译 + 校验后应提交一次。
- **沙箱会丢弃 `.git/refs/remotes/` 的写入**，所以 `git status` 常年显示 `[gone]`——**这是假象，别据此判断没推上去**。核实远端只能调 GitHub API 比对 `repos/<owner>/<repo>/commits/main` 的 sha 与本地 `HEAD`（`git push` 的回显也不可信）。

### 工具链（`.workbuddy/`，除注明外均可**不传参自动取最新一期**）

| 脚本 | 作用 | 何时跑 |
| --- | --- | --- |
| `build_web.py [src] [out]` | md → 单文件网页版 | 出稿后 |
| `verify_web.py [src] [out]` | 六项自查，须 `ALL GREEN` | 编译后 |
| `extract_deadlines.py [src]` | 第四章截止表 → `data/deadlines.csv` | 出刊前 |
| `check_deadlines.py [--days N] [--today D]` | 到期巡检（`--today` 可复现任意基准日） | 出刊前 |
| `check_frozen.py [src]` | 防重复往期，有 🔴 高危则 **exit 1** | 出刊前 |
| `build_source_hits.py [--dry]` | 信源命中 → 回写台账 + CSV（幂等） | 出刊后 |
| `build_index.py` | 重建根目录 `INDEX.md` | 出刊后 |

### 结构化台账（`.workbuddy/data/`）

- `deadlines.csv` —— 截止时间表，**每期从正文重建**（保证与正文一致）。状态分类**关键词优先于色标**：🟢 同时用于"生效中"和"未开始"，只认色标会误判。
- `frozen_items.md` —— 已固化条目库。关键词必须能唯一命中（专有名 + 版本号），泛词会大面积误报。
- `open_questions.md` —— 待澄清口径。**挂满 3 期的必须升级为正式避坑**（Q01 Qoder 已达阈值）。
- `source_hits.csv` —— 信源命中统计。**口径是"品牌被提及"，不等于"实际取料"**，同域名的源会得到相同数字。可用输出是"零命中 124 条"那批（T2 94% / T3 92% 从未被引用）；**不要拿高命中数对外声称覆盖量**。

### 两条边界（都是有意为之，别"顺手"改掉）

- **不做物理归档**（不把旧期挪进子目录）：自动化需跨期回读历史日报来避免重复，挪走会打断它。改用 `INDEX.md` 解决"找得到"，不牺牲"读得到"。
- **编号以「期号」为准**，不用「第 N 次触发」——两者会脱钩（003/004 漏登记时，"第 3 次触发"产出的其实是第 005 期）。**每期执行完必须写记忆，这是硬要求。**

### 改脚本时的两条幂等纪律

- **回写表格要按表头记下的原表列数截断**再补列，不能用"值等于列名"剥列——否则第二次运行会把已写进的 `005`/`3` 当成数据留下，表越跑越宽。
- **不要假设所有表同构**：台账 T3 两张表只有 3 列（无「用途」列），强行补齐会撑坏表。
- 改完务必验幂等：连跑两次 `diff` 必须无输出。已验证：`build_source_hits.py` 连跑三次字节一致。
