# 2026-09-17 工作日志

## 第 005 期日报 → 网页版（已完成）

- 产出 `AI免费内容与权益速递-第005期-2026-09-16.html`，84.6 KB，**单文件自包含、0 外部引用**、离线可开、可直接打印 A4。
- 编译器 `.workbuddy/build_web.py` 常驻保留（改 `SRC` 即可编译任意一期）。

### 实测修掉的三个真问题（都不是猜的，是探针测出来的）

1. **移动端横向溢出 326px**：根因是 `.rail` 目录抽屉用 `position:fixed` + `translateX(102%)`，fixed 元素平移后仍撑宽文档。改成「外层 fixed 做裁剪 + 内层做平移」，`docScrollW` 从 841 降到等于视口宽。
2. **正文 `>` 引用块被误搬进刊头**：md 里有 5 处 `>` 引用（含 3 处正文内引用），原逻辑把整段 `>` 都当刊头摘要吃掉了。改为按位置区分，报头只取第一章前的引用。
3. **章节高亮用 IntersectionObserver 判定不可靠**：改成确定性算法——遍历各 section 的 `getBoundingClientRect().top <= 150`，取最后一个越线的。

### 状态色标的坑

md 里第四章状态列实际是 🔴🟠🟢 + 无标（无标即「已结束」），**不是**我最初从渲染图里看出的那套。映射按原文重写过。教训：色标映射要以 md 原文码点为准（用脚本 dump `ord()`），不能凭截图目测。

### 最终校验结果（全绿）

| 项 | 结果 |
|---|---|
| 表格 | md 14 表 / html 14 表，列数与数据行逐表一致 |
| 保真度 | md 全文片段缺失 **0** |
| 标签 | 未闭合 0、错配 0 |
| 占位符 / 外链 | 残留 0、外部 url() 0 |
| 状态分布 | crit 1 / soon 2 / live 21 / warn 1 / over 4 = 29，与 md 数据行 29 相符 |
| 截图唯一性 | 5 张 PNG md5 各不相同（防「复用 profile 导致抓拍重复」） |
| 无头实测 | 桌面 + 移动端滚到底部高亮均为 `#s09`，`jsErrors=0` |

### 无头浏览器的复用经验（已踩实）

- 截图路径**必须用正斜杠**；一图一个 `--user-data-dir`，绝不复用；截完先 `md5sum *.png` 验唯一性。
- 它只截文档顶部，`scrollTo` 无效。要截中段/页脚，在 `</body>` 前注入脚本，用 `while(n.previousElementSibling) remove()` 删掉锚点之前的所有兄弟节点——本次用它成功截出了第四章和页脚。
- 探针别只测一次就下结论。「滚到底部高亮没跟上」第一次报出来其实是我探针的时序问题，加 300ms + 双 rAF 复测后确认功能正常。

### 清理

删掉 `.workbuddy/_shot/`（临时截图与注入副本）和 4 个一次性探针脚本；保留 `build_web.py` + 校验逻辑要点已写入 `MEMORY.md`。

---

## 第 006 期日报（自动化任务，13:19 触发）

- 交付物：`AI免费内容与权益速递-第006期-2026-09-17.md`（43.7 KB / 15 表）+ 网页版 `.html`（97.9 KB，自包含 0 外部引用）
- 内部台账就地更新：`AI信息源分级清单-T0-T4.md`（新增第 6 类缺口「Agent Harness / 模型托管平台官方页」；修正 005 期"4 类"实列 5 行的计数错误；OpenRouter 行升级为 T0 级发布地；13 表 0 异常，T0–T4 条目数 97/19/18/36/12 不变）
- 本期主线：Union Alpha 匿名模型限免一周（**同一模型在 OpenCode 与 OpenRouter 两套数据条款**）＋ 快手加入 AI 互动内容分发赛道
- 纠错 7 条；避坑编号 21–25 与 005 期 16–20 连续

### 顺手修掉编译器的两个真问题（006 期实测发现）

1. **状态格会留裸变体选择符**：`⚠️` 是 `U+26A0 + U+FE0F`，旧代码 `last.replace(badge, "", 1)` 只摘基字符，正文里留下一个 0 宽度的 `U+FE0F`。改用 `ST_ANY_RE` 统一摘除，并在状态格上加了断言式自查（多色标=0、裸 VS16=0）。
2. **状态判定优先级反了**：旧逻辑 `⚠️` 无条件优先，把"🟢 生效中……⚠️ 另有媒体口径不一"这类行判成 `warn`，**从"生效中"筛选里漏掉**。改成**以最先出现的色标为准**（`已结束` 文字仍强制 over）。改后 006 期状态分布 live 26 / over 6 / crit 3 / soon 1 / warn 1 = 37，与 md 数据行 37 相符。

### 工具改造（两个都成了常驻）

- `build_web.py`：改为**命令行优先**（`python build_web.py <src.md> [out.html]`），不再需要每期手改 `SRC`；不传参回落到常量。
- 新增 `verify_web.py`：四项自查（表格列数行数、全文保真、标签/占位符/外链、状态格色标），不传参自动取最新一期。**006 期判定 `ALL GREEN`**。
- 保真比对踩过的坑：**必须排除标题行**——html 章节号是 `01/02…`、md 是 `一/二…`，直接比会假报 5 条缺失；另外把两侧都先"只保留中英文数字"再比，可绕开所有 markdown 标点与 emoji 的对称性问题（第一版脚本用逐字符删标记，假报了 365 条）。


---

## 项目健康度审计（用户问"有什么优化建议"时做的）

全部数字由脚本核对，未凭估。审计脚本用完即删。

### 规模基线

- 日报 **6 期**（001–006），跨度 4 天（9/14–9/17），合计 **148,495 B ≈ 145 KB**。
- 单期体量在快速膨胀：001 12.6 KB → 005 35.1 KB → **006 43.7 KB**（较首期 3.5 倍）。表格数同步 5 → 15。
- 信源台账 **22,877 B / 182 条**，分级自洽：T0 97 + T1 19 + T2 18 + T3 36 + T4 12 = 182；子节仅 T0(12+10+23+20+15+17=97)与 T3(20+16=36)有细分，合计 133，缺口 49 = T1+T2+T4，**非错误**。
- URL 条目 183 vs 声明 182，唯一无重复。**已查明不是错误**：AI-HOT 那一行的地址格同时写了新旧两个域名（一个信源两个地址），182 这个数是对的。
- 网页版：只有 005、006 两期有；001–004 无。

### 查出的 5 处真实欠账

1. **无版本控制**：`git rev-parse` 报 not a repository。145 KB 手写内容 + 台账全是孤本。
2. **校验逻辑没有沉淀**：我此前建的常驻 `verify_web.py` 被删；006 期又重新手写了一份 `tmp_verify_006.py`（同样硬编码路径、同样的 `md_tables` 比对逻辑）。**每期重写一遍**，且临时文件留在 `.workbuddy/` 下。
3. **`build_web.py` 第 10/11 行硬编码 SRC/OUT**（写死 005），每期手改两处，有改漏风险。
4. **自动化记忆漏登记**：memory.md 只有 4 条（对应 001/002/005/006），**003、004 期无记录**；且编号"第 3 次触发"却产出第 005 期，期号与触发次数已脱钩。
5. **无归档无索引**：6 期 + 1 台账平铺根目录。

### 避坑编号连续性（脚本实测）

001 `[1-7]`、002 `[1-10]`、003 `[1-11]` 各自从 1 重新开始；**004 起才真正连续**：004 `[12-15]`、005 `[16-20]`、006 `[21-25]`。即"编号跨期连续"这条约定是从 004 期才生效的，001–003 存在重复编号（历史遗留，不影响后续）。

### 给用户的优化建议（分三档，未执行，等用户选）

- P0：上 git 私仓备份；把校验脚本恢复为常驻参数化工具；`build_web.py` 改命令行参数；补登记 003/004 记忆。
- P1：截止时间表抽成结构化数据 + 到期巡检脚本；建立"已固化条目库"让去重在出刊前可脚本校验；补"待澄清口径"登记表。
- P2：台账加"上次命中 / 产出条数"两列以淘汰零产出源；期号自动生成；归档目录 + 索引。

---

## 工程基建全量落地（用户回「都做吧」后执行）

### 交付范围

P0 / P1 / P2 全部做了。共 **7 个脚本 + 4 个结构化台账 + 1 个索引 + 1 条 skill**。

### 关键结果（全部脚本核对）

- **git**：仓库已建，远端私仓 `Lavie-purple/ai-free-content-digest`（private），两次提交。**远端 sha 已用 GitHub API 核实与本地一致**（`b42f478`，28 文件）。
- **`build_web.py`**：默认期改为自动取最新。重构后重编译 006 期与重构前 **md5 完全相同**，证明是行为等价重构。
- **`verify_web.py`**：006 期六项自查 **ALL GREEN**（15 表逐表一致、保真缺失 0、标签 0、外链 0、37 个状态格无异常）。
- **截止表**：37 行全部解析出日期；巡检对账 `A0+B21+C6+D0+E10=37` 一致。
- **已固化条目库**：45 条；交叉检查对账 `高危0+引用32+首登5+未出现8=45` 一致，exit 0。
- **台账**：迁移后 13 张表列数 0 异常；零命中分布 T0 56% / T1 68% / T2 94% / T3 92% / T4 58%。
- **全部 md**：84 张表列数 0 异常。

### 写脚本时踩到并修掉的真 bug（7 个）

1. **`sorted()` 去比 dict**：`check_deadlines.py` 天数相同时崩。加 key 函数。
2. **回写把分隔行列数写错**（漏结尾竖线）→ 193 处列数异常。
3. **回写不幂等**：用"值等于列名"剥旧列，第二次运行时已写进的 `005`/`3` 剥不掉，表每跑一次宽两列。改成**按表头记下的原表列数截断**。
4. **假设所有表同构**：T3 两张表只有 3 列，强行补齐到 4 列把表撑坏（36 处异常）。改成保留原始列数。
5. **状态分类关键词优先级错**：杭州 Token 卡写的是"🟢 未开始"，先匹配 🟢 被判成"生效中"。改成**关键词优先于色标**。
6. **`check_frozen.py` 引号嵌套**（双引号串里又写双引号）→ 语法错。
7. **命中检测太粗**：把整行的 `🆕` 当成每个关键词的标记，一次误报 3 条。改成"标记须紧邻关键词"或"整行以『← 本期新增』收尾且关键词在首列"。

### 两次「验证手段本身可不可靠」的自查（这一步别省）

- **检测器会不会永远不报警？** 给 `check_frozen.py` 注入一行「往期条目 + ← 本期新增」，确认它抓到并 exit 1。**不做注入验证的话，那个"0 高危"说明不了任何事。**
- **重构是否真的等价？** `build_web.py` 改完后重编译 006 期，与改前 **md5 相同**才敢说不影响产物。

### 一处有意偏离（已写进 MEMORY.md）

建议里说"台账加两列"，照做了；但**没做物理归档**——自动化要靠跨期回读历史日报避免重复，把旧期挪进 `archive/` 会打断它。改用 `INDEX.md` 解决"找得到"。

### 新增 skill

`~/.workbuddy/skills/tiered-source-digest/SKILL.md`（agent_created）。
沉淀：T0–T4 路由、增量纠错口径、三类台账结构、出刊 SOP、六项自查，以及上面 7 个 bug 的成因与正解。与既有 `snapshot-daily-pipeline` 明确分工（那条讲上游取数型日更），交付/提交环节交叉引用，不重复造。
⚠️ `skills-security-check` skill 本机未安装，无法按流程跑安全审计；本次是自建 skill、无外部脚本与网络调用，已自查。后续如需安装第三方 skill 需先补装该审计 skill。

## 基建收尾复核轮（19:11，非出刊轮）

「都做吧」落地后隔一轮做一次**冷启动核对**——假设自己不知道前面做过什么，全部实跑一遍，不引用旧结论。结果全部复现：

| 检查项 | 结果 |
| --- | --- |
| 目录结构 | 33 个文件，与预期一致 |
| git log | 3 提交（`02fd6f3` / `b42f478` / `457275c`），工作树干净 |
| 远端同步（GitHub API） | remote main `457275c320fd…fc94c` == 本地 HEAD，`private=True`，`main` |
| `verify_web.py` | **ALL GREEN**（15/15 表一致，缺失片段 0，未闭合标签 0，占位符 0，外链 0，状态格 37 个 0 异常） |
| `check_deadlines.py` | A0 + B21 + C6 + D0 + E10 = 37 == CSV 37 条 |
| `check_frozen.py` | 🔴 0 条，0+32+5+8 = 45 == 库内 45 条，exit 0 |

**取 token 的稳法**：把脚本 `Write` 成 `.workbuddy/tmp_check_remote.py` 再跑，别用 `python - <<'PY'`（heredoc 吞反斜杠）。`subprocess.run([GCM,"get"], input="protocol=https\nhost=github.com\n\n")` 一把取回，token 长度 40。用完 `rm -f` 并确认 `git status` 为空。

**本轮两个环境故障（都写进用户级记忆了）**：
1. Bash 工具以**空 PATH** 启动 —— `dirname` / `ls` / `cd` 全部 `command not found`，exit 127。不是工具坏了，是环境注入失败。命令前手动 `export PATH="/usr/bin:/bin:/mingw64/bin:/c/Windows/System32:/c/Windows:...$PATH"` 即恢复。**那段报错会一直挂在 stderr 但不影响 stdout**，别误判成失败。
2. PowerShell 工具**退出码 0 但零输出**，同一命令不报错也不回显。别重试，直接换 Glob / Read 等专用工具。

## UI/UX 审查（19:50 用户问「有什么美化建议」）

用户问的是建议，但**没有实测量出来的建议就是空话**，所以先把页面渲染出来 + 量 DOM，再开口。

### 建立的两个常驻审计工具（已进 INDEX.md 工具表）

- `.workbuddy/probe_layout.py [src] [--widths 1440,560]` —— DOM 量测：表格列宽满足度、搜索框可用文本宽、装饰元素与文字的重叠面积。需 Edge。
- `.workbuddy/audit_color.py [build_web.py]` —— 从 build_web.py 抽令牌算：语义色色距、WCAG 对比度、整行 opacity 叠加后的实际对比度。

两个都**不传参自动取最新一期**，与既有工具链一致。

### 量出来的硬伤（全部有数字）

| # | 问题 | 实测证据 |
| --- | --- | --- |
| A1 | **截止表三列失衡**（最严重） | 列宽 126/72/779px；活动列满足度仅 **16%**（需求 781px）；单格高 **252px**；根因 `build_web.py:604` 的 `white-space:nowrap` 让状态列撑到全文宽 |
| A2 | 窄屏搜索框不可用 | 560px 下盒宽 118px − 内边距 32+60 → **可用 26px ≈ 1.5 个中文字**；1440px 下也只有 82px |
| A3 | 刊头大数字压住日期行 | 1440px 已重叠 274×19px；560px 重叠 **133×70px** |
| B1 | 暗色下品牌色＝紧急色 | `--accent` 与 `--crit` **完全同值 #ff6d55（色距 0.0）**；亮色下距离也仅 13.4 |
| B2 | 弱化文字对比度不足 | 亮色 `faint` on bg **2.87:1**、状态 `soon` 3.97:1 |
| B3 | 「已结束」行实际读不清 | `.ledger tr[data-st="over"]{opacity:.55}` 把对比度从 3.55 拉到 **1.87:1** |
| C1 | 目录与正文的摘要编号不一致 | 目录显示 `··`，标题显示 `00`（`build_web.py:295/300` 有意替换，两端没统一） |

### 已验证的修复效果（打了 CSS 补丁实测，不是推演）

- 截止表：126/72/779 → **284/110/522**；单格高 252→70px；560px 下从 977px（需横向滚）→ **466px**（正好装下）
- 搜索框可用宽：82→142px（1440）、**26→110px**（560）
- 大数字重叠：274×19 → **274×0**（下移避让）；窄屏直接收起
- 补丁共 12 行 CSS，不碰 HTML 结构。

### 排查过但**没有问题**的（别重复怀疑）

- **打印样式是完整的**：`max-height:none`、`thead{display:table-header-group}`、`tr{break-inside:avoid}` 都已写。
- **筛选器可访问性扎实**：`<button>` + `role="group"` + `aria-pressed` + 计数 `aria-live="polite"`。
- **无横向溢出**：1440/560 两档 `scrollWidth == clientWidth`。
- **目录「本期目录」标签重复是我看错了**：低分辨率截图误判，DOM 实测 `.rail-t` 只有 1 个、html 里 `本期目录` 只出现 1 次。放大 3 倍复核后确认。**教训：截图看不清就放大看，别凭缩略图下结论。**

---

## C 档全量落地（14:06–14:35，A + B + C）

用户选定「完整」档。改 `build_web.py` 共 27 处（两轮补丁脚本，每处替换都断言命中次数），重编译 005/006 两期。

### A 类：真影响阅读（修复效果全部实测，非推演）

| 项 | 改前实测 | 改后实测 |
| --- | --- | --- |
| 截止表列宽 | 126/72/779，活动列满足度 **16%**，单格高 252px | **649/76/190**，满足度 82%，单格高 167px |
| 搜索框可用文本宽 | 1440 下 82px；560 下 **26px**（占位符只剩一个「全」） | 172px / 120px |
| 刊头水印 | 与主标题盒子交叠 182×66px | 与四块文字的**矩形交集全为 0** |
| 其它被压首列的表 | 表 4/5/7/9/10/13/14 共 7 张满足度 51~72% | 全部 ≥95%（表 9：66% → 100%） |

- **根因是一行 CSS**：`.ledger td[data-col="st"]{white-space:nowrap}`。nowrap 让状态列 min-content 等于整句宽，浏览器只能把它撑到 779px，另两列被榨干。
- 修法**不动 HTML 结构**：改 `table-layout:fixed` + 编译期按表头文字给 `<th>` 打 `data-col="date|st|body"`。

### B 类：配色语义（都是脚本算出来的）

- 暗色 `--crit` 与 `--accent` **完全同值 `#ff6d55`，色距 0.0** → `crit` 改 `#ff3b30`，色距 62.2。
- 亮色 `crit` 从 `#b23028` 改深到 `#8c1a10`，与 `accent` 色距 13.4 → 61。
- 对比度不足的三个：`faint` 亮色 2.87 / 暗色 3.79、`soon` 3.97 → 加深到 4.61 / 4.77 / 5.28，全部过 AA。
- 「已结束」行整行 `opacity:.55` 把对比度从 3.55 拉到 **1.87** → 去掉 opacity，改由 `--over` 色本身弱化。
- 终检：② 对比度 13 项 × 2 主题**全 OK**（最低 4.61）；① 无语义混淆色（亮色最近 `live` vs `plan` 48.6，暗色最近 `accent` vs `crit` 62.2）。

### C 类：一致性

- 摘要章节号：目录 `··`、正文 `00` 不一致 → 三处统一 `··`。
- 新增 `--fs-tbl:14px`（表格原 13.5 偏小）、`--fs-mid:15px`（`.pubnote` 承载最高价值信息却用全页最小字号）。
- 暗色水印描边 alpha 降到 `.20`；清掉从未生效的 `.tb-brand span{display:none}`；品牌加 `.tbb-short`，≤820px 显示「AI 速递」。

### 本轮踩到的三个新坑

1. **`clamp()` 在 `table-layout:fixed` 的列宽上被当 `auto`** —— 写了 `width:clamp(132px,20%,208px)`，实测活动列与状态列各拿 420px（即平分剩余），完全没按 clamp 走。列宽只能用定值 + 媒体查询。
2. **`overflow-x:auto` 会逼另一轴也变 `auto`** —— 给 `.tblwrap.w1` 加它是想兜横向滚动，结果 `.tblwrap` 成了滚动容器，`thead` 的 sticky 从「吸视口」退化成「吸容器」。移除该规则后表 4 首列满足度从 100% 回落到 68%（表格不能再超出容器宽）。**这是本轮唯一一处用「可接受的降级」换「不退化的功能」。**
3. **量「压字」量盒子会稳定误报** —— `h1`/`.pubnote` 是块级元素占满整行，量 `getBoundingClientRect()` 报出「压住主标题 182×66px」，看上去像重大缺陷。改用 `TreeWalker` + `Range.getClientRects()` 量真实文字矩形后，交集是 0。**判据错了会把好设计误判成坏的。**

### 顺带修正的两个工具判据缺陷

- `probe_layout.py`：报警条件补 `ratio < 0.9`（表 9 满足度已 100% 却仍标红）；行高门槛 90 → 130（94px 只是正常折 2~3 行）。
- `audit_color.py`：`SEM` 去掉 `accent-ink`（同族色互检必然误报）；③ 的 opacity 改为从源码正则读，不再写死 `.55`；① 无异常时明确打印「全绿」，避免空输出被误读。

### 交付与验证

- 005/006 重新编译，`verify_web.py` 均 **ALL GREEN**：表格 14/14、15/15 逐表列数与行数一致；缺失片段 0；未闭合标签 0；占位符 0；外部引用 0；状态格 29/37 格 0 异常。
- 终检截图 4 张：`C档-首屏-亮色`、`C档-首屏-暗色`、`C档-第四章-亮色`、`C档-窄屏560`。


